import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true, // Enabling React Strict Mode
 
  reactDevOverlay: false, // Disable React Dev Overlay
};

export default nextConfig;
